# BanglaBridge Backend Requirements
# Core runtime has 0 external dependencies (runs on Python 3 standard library)!
#
# If you want to enable Tier 1 Meta NLLB-200 Seq2Seq model on Render:
# (Note: Requires a Render plan with >= 4GB RAM to load the 600M parameter model)
# torch
# transformers
# sentencepiece
# indic-nlp-library
