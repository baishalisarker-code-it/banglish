services:
  - type: web
    name: banglabridge-backend
    runtime: python
    buildCommand: ""
    startCommand: "python -m backend.server"
    healthCheckPath: "/api/health"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
